# CALCULA LA SUMA DE LOS NUMEROS NATURALES ENTRE A Y B
# EJECUTAR CON C=1 SIEMPRE
00 ? READ A
01 ? READ B
02 ? READ C
03 ? LOAD A
04 ? STORE D
05 ? LOAD A
06 ? ADD C
07 ? STORE A
08 ? ADD D
09 ? STORE D
10 ? LOAD A
11 ? SUB B
12 ? JZ 14
13 ? JMP 05
14 ? LOAD D
15 ? WRITE D
16 ? HALT
