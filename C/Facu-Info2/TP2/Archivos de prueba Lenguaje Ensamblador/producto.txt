# CALCULA EL PRODUCTO DE DOS NUMEROS
# EJECUTAR CON D=1 SIEMPRE
00 ? READ A
01 ? READ B
02 ? READ D
03 ? LOAD C
04 ? SUB C
05 ? STORE C
06 ? LOAD B
07 ? JZ 15
08 ? LOAD C
09 ? ADD A
10 ? STORE C
11 ? LOAD B
12 ? SUB D
13 ? STORE B
14 ? JMP 07
15 ? WRITE C
16 ? HALT
